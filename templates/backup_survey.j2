{
  "description": "",
  "spec": [
    {
        "question_name": "What date and time for backups?",
        "min": null,
        "max": null,
        "required": true,
        "choices": "{% for item in find_output.files %}{{ item.path }}\n{% endfor %}",
        "variable": "network_target",
        "type": "multiplechoice"
    }
  ],
  "name": ""
}
